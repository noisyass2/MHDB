android {
            defaultConfig {
                applicationId = "org.nativescript.MonsterHunterWorldDB"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }